

def keyboardOutput(agentDirection, newDirection, movement):
    agentDirectionList = [0,90,180,270]
    newDirectionList = ['U','D','R','L']

    if (agentDirection in agentDirectionList) and (newDirection in newDirectionList) and movement >=0:
        

